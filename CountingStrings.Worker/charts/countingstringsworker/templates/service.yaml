apiVersion: v1
kind: Service
metadata:
  name: {{ template "countingstringsworker.fullname" . }}
  labels:
    app: {{ template "countingstringsworker.name" . }}
    chart: {{ template "countingstringsworker.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "countingstringsworker.name" . }}
    release: {{ .Release.Name }}
