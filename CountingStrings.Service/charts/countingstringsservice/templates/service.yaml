apiVersion: v1
kind: Service
metadata:
  name: {{ template "countingstringsservice.fullname" . }}
  labels:
    app: {{ template "countingstringsservice.name" . }}
    chart: {{ template "countingstringsservice.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "countingstringsservice.name" . }}
    release: {{ .Release.Name }}
